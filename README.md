# Лабораторна робота №3  
### **Технології Back-end розроблення · Варіант 2 — Користувацькі категорії витрат**

---

## 📌 Опис роботи

У рамках роботи необхідно створити серверний застосунок на Flask, що підтримує:

- коректну **валідацію даних** (Marshmallow);
- **обробку помилок** на рівні API;
- використання **ORM (Flask-SQLAlchemy)** з підтримкою міграцій (Flask-Migrate);
- **зберігання даних у PostgreSQL**;
- реалізацію **REST-API**, документованого через Flask-Smorest (Swagger `/docs`);
- готовність до деплою на **Render.com**.

---

## 🎯 Мета роботи

Розробити backend-систему для управління витратами, у якій присутні:

- глобальні категорії, доступні кожному користувачу;
- персональні категорії, прив’язані до конкретного користувача;
- контроль доступу до категорій та записів витрат;
- валідація payload'ів та структурована обробка помилок.

---

## 🧩 Особливості варіанта №2 — *Користувацькі категорії витрат*

### Категорії поділяються на:

#### **1. Глобальні**
- `is_global = true`  
- видимі всім, використовувати їх може будь-який користувач.

#### **2. Персональні**
- мають поле `user_id`  
- доступні лише власнику; інші користувачі не можуть:
  - переглядати їх;
  - використовувати при створенні записів витрат.

### Перевірки, що виконуються при створенні записів:

- категорія існує;
- якщо категорія персональна — вона належить саме тому користувачу, який створює запис;
- всі поля проходять Marshmallow-валідацію.

---

## 🛠️ Використані технології

- **Python 3.11**  
- **Flask**, **Flask-SQLAlchemy**, **Flask-Migrate**  
- **Marshmallow**, **Flask-Smorest**  
- **PostgreSQL**  
- **Gunicorn** (для продакшну)

---

## 🚀 Локальний запуск проєкту

```bash
# 1. Створення та активація віртуального середовища
python -m venv venv
venv\Scripts\activate      # Windows
# або
source venv/bin/activate   # Linux/macOS

# 2. Інсталяція залежностей
pip install -r requirements.txt

# 3. Налаштування змінних середовища
set FLASK_APP=app.py
set DATABASE_URL=postgresql://postgres:1234@localhost:5432/lab3_db

# 4. Ініціалізація бази даних
flask db init
flask db migrate -m "Initial migration"
flask db upgrade

# 5. Запуск сервера
python app.py
```

## 🌐 Доступ до API

- **Сервер:** `http://localhost:5000`  
- **Swagger-документація:** `http://localhost:5000/docs`

---

## 📚 Основні REST-ендпоінти

| Метод  | Шлях        | Призначення                                      |
|--------|-------------|-------------------------------------------------|
| **GET**  | `/user`       | Отримати список користувачів                    |
| **POST** | `/user`       | Створити нового користувача                      |
| **GET**  | `/category`   | Отримати категорії (глобальні + персональні)    |
| **POST** | `/category`   | Створити нову категорію витрат                   |
| **GET**  | `/record`     | Переглянути всі записи витрат                     |
| **POST** | `/record`     | Додати новий запис витрат                         |

---

## 📝 Примітка

Для деплою на Render.com потрібно додати `gunicorn` у `requirements.txt` та створити `Procfile`, наприклад:
```
web: gunicorn app:app
```
---

## ✔️ Критерії виконання

- Валідатор коректно обробляє дані.  
- Система міграцій працює.  
- Контроль доступу до категорій реалізовано.  
- API задокументовано та доступне через `/docs`.  
- Застосунок успішно запускається локально.

---